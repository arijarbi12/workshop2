{% extends'base.html.twig' %}
 
{% block title  %}hello ProductController!{% endblock %}

{% block body %}

{{title}}
{{productObject.id}}
{{productObject.name}}
{{productObject.price}}
{% set prixtotale = 0 %}
{% if productsArray |length != 0 %}
<p> Nombre d'auteurs :{{authors|length}}</p>
<table>
    <tr>
        <th>Id</th>
        <th>name</th>
        <th>price</th>
        </tr>
 {% for product in productsArray %}
 {% set prixtotale = prixtotale+product.price %}
 <tr>
    <td>{{product.id}}</td>
    <td>{{product.name|upper}}</td>
    <td>{{product.price}}</td>
</tr>
    {% endfor%}
 </table>
{% else %}
<p>there are no products</p>
{% endif %}
<a href="{{path('showTeacher',{'name':'arij','id':1})}}">Vers teacher</a>
<img src="/images/image1.jpg" width="250" height="250">
{% apply upper%}
prix des produits = {{prixtotale}}
{% endapply%}
{% endblock %}
